/// <reference types="jest" />

import { {{ camelCase name }} } from './{{ camelCase name }}';

describe('{{camelCase name}} test', () => {
  it('exists', () => {
   expect({{camelCase name}}).toBeDefined();
  })
})